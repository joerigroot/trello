import React from "react"

const AddList = () => {
  return (
    <div>
      <form>
        <input type="text" />
        <button>+ Add list</button>
      </form>
    </div>
  );
}

export default AddList